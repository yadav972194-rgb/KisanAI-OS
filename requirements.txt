fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
sqlalchemy==2.0.51
alembic==1.19.1
pydantic-settings==2.15.0
python-dotenv==1.2.2
PyJWT==2.13.0
bcrypt==5.0.0
python-multipart==0.0.32
email-validator==2.3.0

# Local crop-disease image classification (Disease Identification).
# Free / open-source; used only when DISEASE_MODEL_PATH points at an
# ONNX model file. Without a configured model the API stays in the
# safe MODEL_NOT_CONFIGURED state.
onnxruntime==1.28.0
Pillow==12.3.0

# Production database driver (PostgreSQL). Install only where the
# deployed DATABASE_URL uses PostgreSQL; SQLite development is unaffected.
psycopg[binary]==3.3.4

# Production ASGI server (Linux only; the local Windows dev flow uses uvicorn).
gunicorn==26.0.0

# Test dependencies (Phase 5.1)
pytest==9.1.1
httpx==0.28.1
